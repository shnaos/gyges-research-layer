services:
  searxng:
    image: searxng/searxng:latest
    ports:
      - "8080:8080"
    environment:
      - BASE_URL=http://localhost:8080/

  tor-proxy:
    image: dperson/torproxy:latest
    ports:
      - "9050:9050"
    profiles:
      - tor
