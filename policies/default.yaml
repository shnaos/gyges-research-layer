# Deny-by-default capability policy.
# Sprint 1 MVP: the only allowed capability is low-risk `search`
# performed by `local-agent` inside the `research` compartment.
defaultDeny: true
rules:
  - effect: allow
    agentId: local-agent
    compartment: research
    tool: search
    riskLevel: low
    requiresConfirmation: false
