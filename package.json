{
  "name": "gyges-research-layer",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first capability firewall and identity compartmentalization gateway for AI agents performing private web research.",
  "type": "module",
  "scripts": {
    "build": "npm --prefix packages/core run build && npm --prefix packages/policy-engine run build && npm --prefix packages/identity-compartment run build && npm --prefix packages/transport-router run build && npm --prefix packages/search-adapter-searxng run build && npm --prefix packages/agent-sdk run build && npm --prefix apps/grl-server run build && npm --prefix apps/grl-cli run build",
    "build:core": "npm --prefix packages/core run build",
    "build:cli": "npm --prefix apps/grl-cli run build",
    "build:agent-sdk": "npm --prefix packages/agent-sdk run build",
    "test": "vitest run",
    "test:core": "vitest run packages/core",
    "test:cli": "vitest run apps/grl-cli",
    "test:agent-sdk": "vitest run packages/agent-sdk",
    "typecheck": "npm --prefix packages/core run typecheck && npm --prefix packages/policy-engine run typecheck && npm --prefix packages/identity-compartment run typecheck && npm --prefix packages/transport-router run typecheck && npm --prefix packages/search-adapter-searxng run typecheck && npm --prefix packages/agent-sdk run typecheck && npm --prefix apps/grl-server run typecheck && npm --prefix apps/grl-cli run typecheck",
    "typecheck:agent-sdk": "npm --prefix packages/agent-sdk run typecheck",
    "cli": "tsx apps/grl-cli/src/cli.ts",
    "start": "npm --prefix apps/grl-server run dev",
    "dev:server": "npm --prefix apps/grl-server run dev:local-api",
    "build:server": "npm --prefix apps/grl-server run build",
    "lint": "npm run typecheck",
    "smoke-test": "tsx scripts/smoke-test.ts",
    "release-check": "tsx scripts/release-check.ts",
    "validate:exports": "tsx scripts/validate-package-exports.ts",
    "validate:security-docs": "tsx scripts/validate-security-docs.ts",
    "audit:coherence": "tsx scripts/audit-runtime-coherence.ts",
    "audit:mocks": "tsx scripts/audit-mock-boundaries.ts"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.15.30",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9",
    "@types/js-yaml": "^4.0.9"
  }
}
