/**
 * Runtime profile type re-exports.
 *
 * Runtime profile data is surfaced from the GRL Local API. The SDK can read
 * and switch profiles. No raw input, tokens, or secrets are stored here.
 */

export type { RuntimeProfileInfo, RuntimeProfileSwitchResult } from './types.js';
